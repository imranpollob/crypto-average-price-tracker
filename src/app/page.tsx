export default function HomePage() {
  return (
    <main className="container">
      <h1>Crypto Average Price Tracker</h1>
      <p className="status">Not connected</p>
      <p>
        Phase 1 (foundation) is complete: normalized models, provider abstraction, lot engine and
        P/L calculations. Exchange connection and the portfolio dashboard arrive in later phases.
      </p>
    </main>
  );
}
